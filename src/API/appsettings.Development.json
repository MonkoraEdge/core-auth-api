{
  "Serilog": {
    "MinimumLevel": {
      "Default": "Debug",
      "Override": {
        "Microsoft": "Information",
        "Microsoft.Hosting.Lifetime": "Information",
        "Microsoft.EntityFrameworkCore": "Information"
      }
    }
  }
  // SECURITY: Never commit real credentials here.
  // Set POSTGRES_CONNECTIONSTRING via dotnet user-secrets or environment variables:
  //   dotnet user-secrets set "POSTGRES_CONNECTIONSTRING" "Host=localhost; Port=5432; User ID=<user>; Password=<password>; Database=auth-dev;"
}
